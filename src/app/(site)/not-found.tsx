import { Introuvable } from "@/components/Introuvable";

/** `notFound()` appelé depuis une fiche : dans l'habillage des pages à lire. */
export default function IntrouvableSite() {
  return <Introuvable />;
}
